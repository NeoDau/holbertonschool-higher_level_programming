#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    for x in range(len(my_list)):
        my_list[3] = element
        if idx < 0:
            return (my_list)
        else:
            return (my_list)
